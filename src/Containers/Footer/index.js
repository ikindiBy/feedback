import React, { Component } from "react";

import "./Footer.css";

class Footer extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return <footer> site's info | links to social networks </footer>;
  }
}

export default Footer;
