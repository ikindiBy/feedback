import React, { Component } from "react";

import "./Main.css";

class Main extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
      <main>Results of search | companie's page | user's page | ... </main>
    );
  }
}

export default Main;
